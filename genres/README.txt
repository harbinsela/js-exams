Given input field with numbers, select element and button mix!
From json only add the options (Pop,Rock,Blues) once, do not add the duplicates.
n number of tracks is selected in the number input field, and if n is greater than the number of tracks,
all songs from jsonot are displayed, if less then as many songs as n are displayed.
The id is obtained by taking only uppercase letters from Title and Artist and concatenating them
There were two buttons UP and DOWN, clicking UP moves the queue up one position, clicking DOWN moves the queue down
in the table.
When you select a genre, only songs from that genre are added, or if you click all, all songs are added.
The following are added to the table:

id artist title genre releaseYear move.